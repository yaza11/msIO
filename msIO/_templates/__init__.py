"""
Defining the right relationship patterns is crucial for the efficent functioning of the database.

We distinguish the types
- one to many (parent can have many children)
- many to one (
- many to many

We use the declarative form and we use Mapped on type annotations (so we don't need to specify all parameters in
mapped column explicitly
"""

